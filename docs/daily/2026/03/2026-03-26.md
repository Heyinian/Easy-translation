# 2026-03-26

## Summary

将当前 `Easy-translation` 仓库按 project-owned 方式接入 `AIProjectSkeleton` 的开发骨架，并补上最小本地 automated smoke baseline；本次归档检查点版本定为 `v0.5.8`。

## Completed

- 新增 canonical 文档入口：`docs/START_HERE.md`
- 新增 current status / backlog / roadmap / versioning policy
- 新增 repo hygiene 脚本与 GitHub Actions 工作流
- 新增 `config/project/current_project.json`
- 新增 `config/runtime/default.example.json`
- 保留原有产品级开发文档，并改名为 `docs/app_developer_guide.md`
- 调整 README 与现有文档中的开发文档引用
- 为 `config.py` / `settings_manager.py` 增加测试用 runtime 覆盖入口
- 新增 `tests/test_smoke_baseline.py`
- 新增 `scripts/run_smoke_tests.ps1`
- 更新 `docs/TESTING.md` 与 `tests/README.md`

## Verification

- 骨架所需目录、脚本和关键文档已就位
- 当前仓库已声明 `bootstrap_finalized: true`
- `powershell -ExecutionPolicy Bypass -File scripts\run_smoke_tests.ps1` 已通过
- `powershell -ExecutionPolicy Bypass -File scripts\run_repo_hygiene_checks.ps1` 已通过
- 源码版 `.\.venv312\Scripts\python.exe app.py` 可启动，`runtime/logs/triple_space.log` 有最新写入

## Risks / Open Issues

- 当前 smoke baseline 还未接入 hosted CI，也不覆盖 GUI 级自动回归
- 部分 contract 文档仍是通用模板，后续可继续按项目实际收紧
- 现有产品级 Bug 仍需按 backlog 持续处理

## Next Recommended Step

优先处理外部输入框回填后的清选副作用问题，并评估是否把当前 smoke baseline 接入 hosted CI。
