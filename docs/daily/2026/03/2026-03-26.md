# 2026-03-26

## Summary

将当前 `Easy-translation` 仓库按 project-owned 方式接入 `AIProjectSkeleton` 的开发骨架，并补上最小本地 automated smoke baseline；在同一天继续收敛 AI 翻译后续规划，并生成新的 release 检查点 `v0.5.9`。

## Completed

- 新增 canonical 文档入口：`docs/START_HERE.md`
- 新增 current status / backlog / roadmap / versioning policy
- 新增 repo hygiene 脚本与 GitHub Actions 工作流
- 新增 `config/project/current_project.json`
- 新增 `config/runtime/default.example.json`
- 保留原有产品级开发文档，并改名为 `docs/app_developer_guide.md`
- 调整 README 与现有文档中的开发文档引用
- 为 `config.py` / `settings_manager.py` 增加测试用 runtime 覆盖入口
- 新增 `tests/test_smoke_baseline.py`
- 新增 `scripts/run_smoke_tests.ps1`
- 更新 `docs/TESTING.md` 与 `tests/README.md`
- 新增 `docs/ai_translation_profile_plan.md`，整理 AI 场景翻译 profile 与官方 LLM provider 的实现方案和边界
- 在 `docs/backlog.md` / `docs/PROJECT_STATUS.md` 中登记 `ET-FEAT-002`、`ET-FEAT-003` 与对应规划项
- 追加登记输入法式翻译候选词悬浮窗方案，收敛为默认关闭、AI provider 专用、最多 3 个候选和长文本自动回退
- 将 `ET-FEAT-003` 收敛为 `BYOK` 方案，明确测试阶段不限定为 `OpenAI`，可使用兼容 provider preset 或自定义兼容端点
- 同步版本落点到 `v0.5.9`，准备重新打包 Windows release 产物
- 使用 `PyInstaller` 基于 `Easy-translation.spec` 重新生成 `dist/Easy-translation.exe`
- 生成 `dist/Easy-translation-windows-x64.zip`，用于远端 Release 上传

## Verification

- 骨架所需目录、脚本和关键文档已就位
- 当前仓库已声明 `bootstrap_finalized: true`
- `powershell -ExecutionPolicy Bypass -File scripts\run_smoke_tests.ps1` 已通过
- `powershell -ExecutionPolicy Bypass -File scripts\run_repo_hygiene_checks.ps1` 已通过
- 源码版 `.\.venv312\Scripts\python.exe app.py` 可启动，`runtime/logs/triple_space.log` 有最新写入
- 打包版 `dist\Easy-translation.exe` 可启动，`dist\runtime\logs\triple_space.log` 有最新写入
- `Easy-translation.exe` SHA256：`44DE6EA2CFFA6AD639F9B0B841ADCAD411DEF7B2CB4D6C34DCB2C3E0BDBD5F2B`
- `Easy-translation-windows-x64.zip` SHA256：`77CB84E1E6195321761F312BEFD7F2A5F6BACBB484D14A7FF291611BD2E58E8C`

## Risks / Open Issues

- 当前 smoke baseline 还未接入 hosted CI，也不覆盖 GUI 级自动回归
- 部分 contract 文档仍是通用模板，后续可继续按项目实际收紧
- 现有产品级 Bug 仍需按 backlog 持续处理
- AI 场景翻译 profile / 官方 LLM provider 目前只完成方案收敛，尚未开始代码实现
- 候选词悬浮窗模式目前也只完成需求收敛，尚未开始实现

## Next Recommended Step

优先处理外部输入框回填后的清选副作用问题，并评估是否把当前 smoke baseline 接入 hosted CI。
