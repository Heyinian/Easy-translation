# Config Directory

This directory holds example configuration layers for adapting the skeleton to a concrete project.

Current example layers:
- `config/runtime/`
- `config/project/`
- `config/profiles/`

Do not commit machine-local secrets or real production credentials here.
